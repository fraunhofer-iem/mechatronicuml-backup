/**
 *
 * $Id$
 */
package org.muml.testlanguage.validation;

import java.util.Map;

import org.eclipse.emf.common.util.EList;

import org.eclipse.emf.ecore.EObject;
import org.muml.testlanguage.Input;
import org.muml.testlanguage.Output;
import org.muml.testlanguage.specification.NodeSpecification;

/**
 * A sample validator interface for
 * {@link org.muml.testlanguage.Node}. This doesn't really
 * do anything, and it's not a real EMF artifact. It was generated by the
 * org.eclipse.emf.examples.generator.validator plug-in to illustrate how EMF's
 * code generator can be extended. This can be disabled with -vmargs
 * -Dorg.eclipse.emf.examples.generator.validator=false.
 */
public interface NodeValidator {
	boolean validate();

	boolean validateInputs(EList<Input> value);

	boolean validateOutputs(EList<Output> value);

	boolean validateLabel(String value);

	boolean validateSpecification(NodeSpecification value);

	boolean validateSpecificationClassName(String value);

	boolean validateInputCache(Map<String, EObject> value);

	boolean validateOutputCache(Map<String, EObject> value);
}
