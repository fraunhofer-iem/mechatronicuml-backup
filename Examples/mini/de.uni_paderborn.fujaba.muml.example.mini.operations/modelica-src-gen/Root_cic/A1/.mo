	within Root_cic.A1.;
model 
	
		// the behavior of the atomic component
		Invalidinvalid.Invalidinvalid behavior
			annotation(Placement(transformation(extent={{0,0},{40,30}})));
		equation
			
			
	annotation (Icon(coordinateSystem(extent = {{, },{, }})));
end ;
